[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "soothe-sdk"
version = "0.3.2"
description = "SDK for building Soothe plugins and client utilities - restructured module architecture (breaking change)"
readme = "README.md"
license = { text = "MIT" }
requires-python = ">=3.10,<3.15"
keywords = ["soothe", "sdk", "plugins", "agents", "ai", "llm", "websocket", "client"]
classifiers = [
    "Development Status :: 4 - Beta",
    "Intended Audience :: Developers",
    "License :: OSI Approved :: MIT License",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Programming Language :: Python :: 3.14",
    "Topic :: Scientific/Engineering :: Artificial Intelligence",
    "Topic :: Software Development :: Libraries :: Python Modules",
]
dependencies = [
    "pydantic>=2.0.0,<3.0.0",
    "langchain-core>=1.2.0,<2.0.0",  # For message types
    "websockets>=12.0",              # For WebSocket client (v0.2.0)
]

[project.optional-dependencies]
dev = [
    "pytest>=8.0.0",
    "pytest-asyncio>=1.3.0",
    "pytest-cov",
    "ruff>=0.12.0",
    "mypy>=1.0.0",
]

[project.urls]
Homepage = "https://github.com/caesar0301/soothe"
Documentation = "https://soothe.readthedocs.io"
Repository = "https://github.com/caesar0301/soothe"

[tool.hatch.build.targets.wheel]
packages = ["src/soothe_sdk"]

[tool.hatch.build.targets.sdist]
include = ["src/soothe_sdk/"]
exclude = [
    "**/__pycache__",
    "**/*.pyc",
    "**/*.pyo",
]

[tool.ruff]
line-length = 100
target-version = "py310"

[tool.ruff.lint]
select = ["E", "F", "I", "N", "W", "UP"]
ignore = ["E501"]

[tool.ruff.format]
docstring-code-format = true

[tool.mypy]
python_version = "3.10"
warn_return_any = true
warn_unused_configs = true
disallow_untyped_defs = true

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]
